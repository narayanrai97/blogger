<h1>Photos (<%= @photos.count %>)</h1>

<!--<ul id="photos">-->
<!--    < @photos.each do |photo| %>-->
<!--        <li>-->
<!--            <h3>< link_to photo.title, photo %></h3>-->
            
<!--            <p style="font-style: italic; color: black;font-size: 15px">-->
<!--                Added on < photo.created_at.strftime("%B %d, %Y") %>-->
                <!--(< photo.comments.count %> comments)<br/>-->
<!--            </p>-->
<!--        </li>-->
<!--    < end %>-->
<!--</ul>-->

<div class="row">
  <% @photos.each do |photo| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag photo.image.url(:medium), class: 'image'), photo %>
      </div>
    </div>
  <% end %>
</div>


<% if logged_in? %>
    <%= link_to "New Photo", new_photo_path %>
<% end %>